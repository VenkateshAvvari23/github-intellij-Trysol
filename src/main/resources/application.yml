server:
  port: 9000
spring:
  application:
   name: "Trysol"
  datasource:
    url: jdbc:mysql://localhost:3306/irr
    username: root
    password: Venky@2080
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
